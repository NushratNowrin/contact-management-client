import React, { useContext } from 'react';
import { Link } from 'react-router-dom';

const Main = () => {
    return (
        <div className=''>
            <Link to='/home'>Contact Management System</Link>
        </div>
    );
};

export default Main;